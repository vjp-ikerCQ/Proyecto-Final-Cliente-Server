export async function testMongoConnection() {
  console.log('🧪 Simulando prueba de conexión a MongoDB Atlas...');
  console.log('✅ En producción, esto se haría desde un backend');
  console.log('📊 URI configurada:', 'No se muestra por seguridad');
  
  return { success: true, message: 'Prueba simulada - Backend necesario' };
}